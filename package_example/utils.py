# utils is a module inside package_example package
# this is used in app.py which is inside package_example but can be anywhere
# to create a package __init__.py is required


def display(text):
    print(text)
